{% macro render_pattern(element) -%}
<pattern {{- render_attributes(element, ['id']) -}}>
    {% for variable in element.variables %}
        {{- render_element(variable) }}
    {% endfor %}
    {% for rule in element.rules %}
        {{- render_element(rule) }}
    {% endfor %}
</pattern>
{%- endmacro %}
